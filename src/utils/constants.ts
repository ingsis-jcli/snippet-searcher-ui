export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "https://snippetsearcherjcli.duckdns.org/"
export const BACKEND_URL = process?.env?.BACKEND_URL ?? "https://snippetsearcherjcli.duckdns.org/api"
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? "test2@gmail.com"
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? "test2test2!"
